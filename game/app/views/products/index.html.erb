<% if current_user %>
 number of friends: <%= current_user.friends_count %>
<% end %>
<h1>products</h1>

<ul class="thumbnails">
<% @products.each do |product| %>
 <li class="span3">
    <div class="thumbnail">
      <div class="entry">
   
        <%= image_tag( product.image , :size => "250x125") %>
        <div class="caption">
           <h3><%= product.name %></h3>
           <div class="price_line">
            <span class="price"><%= number_to_currency product.price.to_d/100.0 %></span>
            </div>
           </div>
    <%= link_to 'Show', product %>
    
         </div>
        </div>
        </li>  
<% end %>
</ul>

<br />

<%= link_to 'New Product', new_product_path %>
